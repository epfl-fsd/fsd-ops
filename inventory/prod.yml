all:
  hosts:
    openshift_namespace_prod:
      ansible_connection: local
      ansible_host: localhost
      ansible_python_interpreter: "{{ ansible_playbook_python }}"
      openshift_namespace: svc0176p-isas-fsd
      domain: epfl.ch
